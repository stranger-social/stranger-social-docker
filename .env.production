# Production environment configuration for Mastodon
# This file is sourced by Mastodon when RAILS_ENV=production

RAILS_ENV=production
NODE_ENV=production

# These should match the values in .env
# They are included here for reference and can override .env values if needed

# If you need to specify environment-specific production settings, add them here.
# Most configuration should be in .env above.
